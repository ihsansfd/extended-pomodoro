<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
        <Style x:Key="BaseVerticalSeparatorStyle" TargetType="Separator">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="{TemplateBinding Panel.Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    <Style x:Key="VerticalSeparatorStyle" TargetType="Separator" BasedOn="{StaticResource BaseVerticalSeparatorStyle}">
        <Setter Property="Background" Value="{StaticResource Black-50}"/>
    </Style>

    <Style x:Key="ScrollViewerStyle" TargetType="ScrollViewer">
        <Style.Resources>
            <Style TargetType="ScrollBar">
                <Setter Property="Margin" Value="20 0 0 0"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
            </Style>
        </Style.Resources>
    </Style>

</ResourceDictionary>